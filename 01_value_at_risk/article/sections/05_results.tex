% --- File: sections/05_results.tex ---

% NOTE FOR DEV PIPELINE: Metrics are injected dynamically via
% \input{../results/metrics.tex} in main.tex.
% All \newcommand macros below come from that file.

The empirical evaluation was conducted over the full out-of-sample expanding-window backtest, matching the regime diversity of the BTC/USD dataset. Models were evaluated on their ability to forecast the 1-day ahead 99\% Value-at-Risk ($\text{VaR}^{0.01}$), using the Kupiec Proportion of Failures (POF) test \cite{kupiec1995techniques,christoffersen1998evaluating} as the primary statistical acceptance criterion.

\subsection{Model Safety: Kupiec Backtesting}

The primary constraint for any institutional risk model is calibration — the realized breach rate must be statistically consistent with the theoretical 1\% exceedance probability. Table~\ref{tab:results} summarizes the Kupiec test outcomes for each architecture.

The \textbf{Naive LSTM} (\JustTraintheModelBreachRate{} breach rate, \JustTraintheModelKupiec{}) illustrates the core failure mode of unconstrained neural networks on financial time series. Without any structural prior, the model learned to output a near-constant, deeply pessimistic VaR of \JustTraintheModelMeanVaR{} — technically producing zero breaches but reserving far more capital than the risk actually warranted. The Kupiec test rejects this model not for being \emph{too risky}, but for being \emph{statistically inaccurate}: a 0\% breach rate when 1\% is expected is as much a calibration failure as a 5\% breach rate.

The \textbf{Historical VaR Baseline} (\HistoricalVaRBreachRate{} breach rate, \HistoricalVaRKupiec{}) passes the Kupiec test, confirming it as a valid statistical model. However, it suffers from the well-documented ``ghost effect'': by anchoring entirely to the trailing window of realized returns, it reacts to volatility only after it has materialized, lagging the true risk environment.

The proposed \textbf{Anchored LSTM} (\PhysicsInformedBreachRate{} breach rate, \PhysicsInformedKupiec{}) passes the Kupiec test with a breach rate closest to the theoretical 1\% target. The physics-informed prior prevents the over-conservatism of the unconstrained LSTM while preserving adaptive, forward-looking dynamics — a balance the purely historical approach cannot achieve.

\subsection{Capital Efficiency}

While model safety is non-negotiable, the secondary mandate is capital efficiency. A model that chronically overestimates risk traps liquidity and drags down the firm's Return on Equity (ROE).

As visualized in Figure~\ref{fig:var_forecast}, the Naive LSTM exhibits severe ``capital drag'' — its flat, extreme VaR forecast of \JustTraintheModelMeanVaR{} provides no adaptation to changing volatility regimes. The Anchored LSTM, by contrast, dynamically tightens VaR bands during low-volatility periods while maintaining structural stability during market stress, yielding a Mean VaR of \PhysicsInformedMeanVaR{}.

This represents a \textbf{\capEfficiencyBps{} reduction} in required capital reserves relative to the Naive LSTM baseline, without sacrificing statistical validity. The Historical VaR Baseline achieves a slightly lower Mean VaR (\HistoricalVaRMeanVaR{}), but this metric is accompanied by a higher breach rate and the known lag-sensitivity that makes it unsuitable for regime-switching assets.

% --- Dynamic Table Injection ---
\begin{table}[htbp]
    \centering
    \caption{Out-of-Sample Backtesting Results (99\% VaR, Kupiec POF Test)}
    \vspace{0.5em}
    \begin{tabular}{lcccc}
        \toprule
        \textbf{Model Architecture} & \textbf{Breaches} & \textbf{Breach Rate} & \textbf{Kupiec Result} & \textbf{Mean VaR} \\
        \midrule
        Naive LSTM          & \JustTraintheModelBreaches  & \JustTraintheModelBreachRate  & \JustTraintheModelKupiec  & \JustTraintheModelMeanVaR \\
        Historical VaR      & \HistoricalVaRBreaches      & \HistoricalVaRBreachRate      & \HistoricalVaRKupiec      & \HistoricalVaRMeanVaR \\
        \textbf{Anchored LSTM (Proposed)} & \PhysicsInformedBreaches & \PhysicsInformedBreachRate & \PhysicsInformedKupiec & \PhysicsInformedMeanVaR \\
        \bottomrule
    \end{tabular}
    \label{tab:results}
\end{table}

% --- Dynamic Figure Injection ---
\begin{figure}[htbp]
    \centering
    \includegraphics[width=\textwidth]{compare_results.png}
    \caption{Rolling 99\% VaR Forecasts over the out-of-sample window for the Naive LSTM, Historical VaR baseline, and Anchored LSTM. The Anchored LSTM adapts to volatility regimes while maintaining Kupiec-validated safety bounds against realized BTC/USD log-returns.}
    \label{fig:var_forecast}
\end{figure}

\subsection{Why Not Simply Use Historical VaR?}

A pragmatic risk manager reviewing Table~\ref{tab:results} would rightly ask: if both models pass the Kupiec test and their mean VaR figures are within \HistoricalVaRMeanVaR{} versus \PhysicsInformedMeanVaR{}, why invest in the additional complexity of an Anchored LSTM? The aggregate statistics are deliberately close---that is the point. The case for the Anchored LSTM rests on four dimensions that summary tables do not capture.

\textbf{(1) Calibration precision.} The Anchored LSTM achieves a breach rate of \PhysicsInformedBreachRate{}, closer to the theoretical 1\% target than Historical VaR's \HistoricalVaRBreachRate{}. While both pass the Kupiec POF test, the Anchored LSTM's superior calibration leaves a larger statistical buffer before the model would breach the acceptance boundary---a material consideration when the underlying asset exhibits the volatility clustering observed in BTC/USD.

\textbf{(2) Temporal stability.} Historical VaR produces structurally discontinuous VaR paths due to the ghost effect: a single large loss event inflates the VaR estimate for exactly $W$ trading days, then exits the window abruptly, creating a sudden artificial drop in required capital. This cliff-edge behaviour is visible in Figure~\ref{fig:var_forecast}. The Anchored LSTM, by learning volatility decay dynamics endogenously, produces a smoother, economically-consistent VaR path that decays as market stress subsides---reducing unnecessary capital drag during mean-reversion without creating abrupt step-downs.

\textbf{(3) Cross-asset generalizability.} Historical VaR is parameterized solely by the window length $W$, which must be re-optimized for each new asset class or volatility regime. The Anchored LSTM, once trained, embeds a general representation of tail-risk dynamics that can be fine-tuned across instruments with minimal additional calibration. For an institutional portfolio spanning thousands of instruments, this generalization property translates directly into reduced model maintenance overhead.

\textbf{(4) Architectural extensibility.} Most critically, the Anchored LSTM is not a terminal design---it is a foundation. Historical Simulation cannot naturally extend beyond a point-estimate VaR; it has no mechanism to forecast the full return distribution. The Anchored LSTM, as a parameterized neural architecture, is the natural precursor to a Mixture Density Network (MDN) that forecasts the complete conditional distribution of returns. This transition---from regulatory VaR to full density forecasting---is the subject of our next project and represents the true long-term value of investing in a trainable, constrained neural baseline over a static lookup table.