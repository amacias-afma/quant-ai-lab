[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "new_model"
version = "0.1.0"
description = "A new model for Quant AI Lab"
authors = [{ name = "AFMA Lab" }]
dependencies = [
    # "pandas",
    # "scikit-learn"
]

[tool.setuptools.packages.find]
where = ["src"]
